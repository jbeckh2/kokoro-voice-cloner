# Core audio processing (all have binary wheels on Python 3.10-3.12)
librosa>=0.10.0
soundfile>=0.12.0
noisereduce>=3.0.0
numpy>=1.23.0

# CPU-only torch — install with the special index URL, not plain pip install torch:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchaudio>=2.0.0

# ── NOT included (build from source, fail on Python 3.13+) ───────────────────
# kokoro     — pulls in misaki→spacy→thinc→blis (needs Clang/MSVC; no 3.13+ wheel)
# resemblyzer — pulls in webrtcvad (C extension; no 3.13+ wheel)
#
# extract_embedding.py finds kokoro from C:\Code\Text2Speech automatically.
# If not found, it falls back to a torchaudio mel-spectrogram approach.
